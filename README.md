# Usage

Install dependencies:

```sh
$ yarn
```

Copy .envrc.example to .envrc and set ALCHEMY_URL:

```sh
$ cp .envrc.example .envrc
```

Run direnv allow

```sh
$ direnv allow
```

Now start fork

```sh
$ yarn start
```